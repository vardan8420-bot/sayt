# 🚀 Деплой на Vercel - Быстрая настройка

## Автоматический деплой из GitHub

Vercel автоматически деплоит ваш сайт при каждом push в GitHub!

### 1. Подключите GitHub репозиторий к Vercel

1. Откройте: https://vercel.com/new
2. Нажмите **"Import Git Repository"**
3. Выберите ваш репозиторий: `vardan84/sayt`
4. Vercel автоматически определит Next.js
5. Нажмите **"Deploy"**

### 2. Настройте переменные окружения в Vercel

После первого деплоя:

1. Зайдите в проект на Vercel
2. **Settings** → **Environment Variables**
3. Добавьте все необходимые переменные:

**Обязательные:**
- `DATABASE_URL` - URL базы данных PostgreSQL
- `NEXTAUTH_SECRET` - Секретный ключ (сгенерируйте: `openssl rand -base64 32`)
- `NEXTAUTH_URL` - URL вашего сайта на Vercel (будет автоматически)

**Опциональные:**
- `MEILISEARCH_HOST` - URL Meilisearch
- `MEILISEARCH_MASTER_KEY` - Ключ Meilisearch
- `STRIPE_SECRET_KEY` - Stripe секретный ключ
- `STRIPE_PUBLISHABLE_KEY` - Stripe публичный ключ
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`
- `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET`
- `OPENAI_API_KEY` - для AI функций
- `SENTRY_DSN` - для мониторинга ошибок

### 3. После деплоя

Vercel автоматически:
- ✅ Соберёт Next.js приложение
- ✅ Задеплоит на CDN
- ✅ Выдаст URL (например: `sayt.vercel.app`)
- ✅ Настроит HTTPS

### 4. Настройте домен (опционально)

1. **Settings** → **Domains**
2. Добавьте свой домен
3. Настройте DNS записи

---

**Готово! Ваш сайт будет доступен онлайн! 🎉**

