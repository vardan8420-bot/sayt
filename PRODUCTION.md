# Production Deployment Guide

Руководство по развертыванию проекта в продакшене.

## 📋 Требования перед деплоем

### 1. Environment Variables (Переменные окружения)

Убедитесь, что все переменные окружения настроены на вашем хостинге (Vercel, etc.):

**Обязательные:**
- `DATABASE_URL` - URL базы данных PostgreSQL
- `NEXTAUTH_SECRET` - Секретный ключ для NextAuth (сгенерируйте: `openssl rand -base64 32`)
- `NEXTAUTH_URL` - URL вашего сайта (например: `https://sayt.vercel.app`)

**Рекомендуемые:**
- `STRIPE_SECRET_KEY` - Для платежей
- `STRIPE_WEBHOOK_SECRET` - Для вебхуков Stripe
- `UPLOADTHING_SECRET` - Для загрузки файлов
- `UPSTASH_REDIS_REST_URL` - Для rate limiting (опционально)
- `UPSTASH_REDIS_REST_TOKEN` - Токен для Upstash Redis
- `SENTRY_DSN` - Для мониторинга ошибок

См. `.env.example` для полного списка.

### 2. База данных

1. Создайте PostgreSQL базу данных (рекомендуется Neon, Supabase, или Vercel Postgres)
2. Примените миграции Prisma:
   ```bash
   npx prisma migrate deploy
   ```
   или
   ```bash
   npm run db:push
   ```

### 3. Build проверка

Перед деплоем убедитесь, что проект собирается:
```bash
npm run build
```

### 4. Health Check

После деплоя проверьте health check endpoint:
```
GET /api/health
```

Должен вернуть `{"status":"healthy",...}`

## 🚀 Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Настройте переменные окружения в настройках проекта
3. Настройте автоматические деплои из `main` ветки
4. Убедитесь, что установлена правильная версия Node.js (>=18.0.0)

## 🔒 Безопасность

### Заголовки безопасности
Уже настроены в `next.config.js`:
- X-Frame-Options: SAMEORIGIN
- X-Content-Type-Options: nosniff
- Referrer-Policy
- Permissions-Policy

### Rate Limiting
Включено для всех API endpoints. Настройте Upstash Redis для продакшена.

### HTTPS
Vercel автоматически предоставляет SSL сертификаты.

## 📊 Мониторинг

### Health Check
- Endpoint: `/api/health`
- Проверяет: база данных, переменные окружения

### Error Monitoring
Настройте Sentry для отслеживания ошибок:
1. Создайте проект на sentry.io
2. Добавьте `SENTRY_DSN` в переменные окружения
3. Ошибки будут автоматически отслеживаться

### Логирование
Vercel автоматически собирает логи. Доступны в панели Vercel Dashboard.

## 🗄️ База данных

### Миграции
```bash
# Production миграции
npx prisma migrate deploy

# Или push схемы
npx prisma db push
```

### Резервное копирование
Настройте автоматические бэкапы базы данных через ваш провайдер:
- Neon: автоматические бэкапы
- Supabase: ежедневные бэкапы
- Vercel Postgres: автоматические снимки

## ⚡ Производительность

### Оптимизация
- Next.js автоматически оптимизирует изображения
- Статические страницы кэшируются
- API routes имеют rate limiting

### CDN
Vercel автоматически использует CDN для статических ресурсов.

## 🔄 Обновления

### Деплой обновлений
1. Закоммитьте изменения в `main` ветку
2. Vercel автоматически создаст preview deployment
3. После проверки merge в `main` для production деплоя

### Миграции базы данных
Перед деплоем кода, который требует миграции:
1. Создайте миграцию: `npx prisma migrate dev --name migration-name`
2. Проверьте миграцию локально
3. Примените на продакшене: `npx prisma migrate deploy`

## 🐛 Troubleshooting

### Ошибки при деплое
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все переменные окружения настроены
3. Проверьте совместимость версии Node.js

### Ошибки базы данных
1. Проверьте `DATABASE_URL`
2. Убедитесь, что миграции применены
3. Проверьте подключение через `/api/health`

### Rate Limit ошибки
Если используете in-memory rate limiting, настройте Upstash Redis для продакшена.

## 📝 Чек-лист перед запуском

- [ ] Все переменные окружения настроены
- [ ] База данных создана и миграции применены
- [ ] Health check endpoint работает
- [ ] Stripe настроен (если используется)
- [ ] UploadThing настроен (если используется)
- [ ] Sentry настроен (рекомендуется)
- [ ] Тесты пройдены (`npm run test:unit`)
- [ ] Build успешно (`npm run build`)
- [ ] SSL сертификат активен (автоматически на Vercel)
- [ ] Резервное копирование БД настроено
- [ ] Мониторинг ошибок настроен

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в Vercel Dashboard
2. Проверьте Sentry для ошибок
3. Используйте `/api/health` для диагностики

