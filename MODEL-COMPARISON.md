# Сравнение моделей Product

## 📊 Вариант 1: Исходная модель (сложная)

```prisma
model Product {
  id            String      @id @default(cuid())
  name          String
  slug          String      @unique
  description   String      @db.Text
  price         Decimal     @db.Decimal(10, 2)
  comparePrice  Decimal?    @db.Decimal(10, 2)
  sku           String?     @unique
  stock         Int         @default(0)
  images        String[]
  categoryId    String
  category      Category    @relation(...)
  sellerId     String
  seller        User        @relation(...)
  status        ProductStatus @default(DRAFT)
  featured      Boolean     @default(false)
  rating        Float       @default(0)
  reviewCount   Int         @default(0)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  // + ProductAttribute[]
}
```

### ✅ Плюсы:
- Полная функциональность маркетплейса
- SEO-friendly (slug)
- Управление складом (stock, sku)
- Рейтинги и отзывы встроены
- Статусы товаров
- Featured товары
- Сравнение цен

### ❌ Минусы:
- Слишком сложная для начала
- Много полей, которые могут не понадобиться
- Трудно поддерживать на старте

---

## 📊 Вариант 2: Простая модель (ваша)

```prisma
model Product {
  id          String   @id @default(cuid())
  title       String
  description String
  price       Int
  images      String[] // массив URL картинок
  sellerId    String
  published   Boolean  @default(false)
  createdAt   DateTime @default(now())
}
```

### ✅ Плюсы:
- Очень простая
- Быстро разрабатывать
- Легко понять
- Минимум полей

### ❌ Минусы:
- `Int` для цены - нет копеек/центов
- Нет `updatedAt` - нельзя отследить изменения
- Нет связей с категориями, заказами, отзывами
- Нет SEO (slug)
- Нет управления складом
- Ограниченная функциональность

---

## 📊 Вариант 3: Гибридная модель (текущая)

```prisma
model Product {
  id          String   @id @default(cuid())
  title       String
  description String   @db.Text
  price       Decimal  @db.Decimal(10, 2)
  images      String[]
  sellerId    String
  seller      User     @relation(...)
  published   Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Связи
  category    Category?
  orderItems  OrderItem[]
  cartItems   CartItem[]
  reviews     Review[]
}
```

### ✅ Плюсы:
- **Баланс простоты и функциональности**
- `Decimal` для точных цен (копейки/центы)
- `@db.Text` для длинных описаний
- `updatedAt` для отслеживания изменений
- Все необходимые связи сохранены
- Можно легко расширить позже
- Готова для маркетплейса

### ❌ Минусы:
- Чуть сложнее простой модели
- Но все равно проще исходной

---

## 🏆 Рекомендация: **Гибридная модель (Вариант 3)**

### Почему гибридная лучше:

1. **Для денег нужен Decimal**
   - `Int` не поддерживает копейки/центы
   - `Decimal` - стандарт для финансовых операций
   - Stripe, платежи требуют точных сумм

2. **Связи критичны для маркетплейса**
   - Без связей нельзя создать заказы
   - Нельзя добавить товар в корзину
   - Нельзя оставить отзыв

3. **updatedAt полезен**
   - Отслеживание изменений товаров
   - Аналитика, логирование
   - Полезно для админки

4. **Можно расширить позже**
   - Легко добавить `slug`, `stock`, `sku` когда понадобится
   - Не нужно переделывать структуру

5. **Готова к production**
   - Работает с платежами (Stripe)
   - Работает с заказами
   - Работает с корзиной
   - Работает с отзывами

---

## 📈 Сравнительная таблица

| Критерий | Вариант 1 | Вариант 2 | Вариант 3 (Гибрид) |
|----------|-----------|-----------|-------------------|
| Простота | ❌ Сложно | ✅ Очень просто | ✅ Просто |
| Точность цен | ✅ Decimal | ❌ Int | ✅ Decimal |
| Связи | ✅ Все | ❌ Нет | ✅ Все |
| Готовность | ✅ Полная | ❌ Базовая | ✅ Готова |
| Расширяемость | ✅ Да | ⚠️ Ограничена | ✅ Да |
| Для маркетплейса | ✅ Да | ❌ Нет | ✅ Да |
| Поддержка | ⚠️ Сложно | ✅ Легко | ✅ Легко |

---

## 🎯 Итог

**Гибридная модель (Вариант 3)** - лучший выбор, потому что:
- ✅ Не слишком сложная
- ✅ Не слишком простая
- ✅ Готова для реального маркетплейса
- ✅ Можно расширять по мере необходимости
- ✅ Соответствует best practices

**Вариант 1** - слишком сложный для старта, но хорош для зрелого проекта.

**Вариант 2** - слишком простой, не подходит для маркетплейса (нет связей, нет точных цен).

