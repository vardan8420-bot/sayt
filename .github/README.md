# GitHub Actions и автоматизация

Этот проект использует GitHub Actions для автоматизации проверок, тестирования и деплоя.

## 🚀 Доступные Workflows

### 1. CI (Непрерывная интеграция)
**Файл:** `.github/workflows/ci.yml`

**Что делает:**
- ✅ Проверка линтера (ESLint)
- ✅ Проверка типов (TypeScript)
- ✅ Запуск unit-тестов
- ✅ Запуск E2E-тестов (опционально)
- ✅ Сборка приложения
- ✅ Проверка безопасности (npm audit)

**Когда запускается:**
- При каждом push в `main` или `develop`
- При создании Pull Request в `main` или `develop`

### 2. Deploy (Деплой)
**Файл:** `.github/workflows/deploy.yml`

**Что делает:**
- Собирает приложение
- Деплоит на Vercel (если настроено)

**Когда запускается:**
- При push в `main`
- Вручную через GitHub Actions UI

**Требуемые Secrets:**
- `DATABASE_URL`
- `NEXTAUTH_SECRET`
- `NEXTAUTH_URL`
- `MEILISEARCH_HOST`
- `MEILISEARCH_MASTER_KEY`
- `VERCEL_TOKEN` (опционально)
- `VERCEL_ORG_ID` (опционально)
- `VERCEL_PROJECT_ID` (опционально)

### 3. CodeQL Analysis (Анализ безопасности)
**Файл:** `.github/workflows/codeql.yml`

**Что делает:**
- Анализирует код на уязвимости
- Проверяет безопасность зависимостей

**Когда запускается:**
- При каждом push в `main` или `develop`
- При создании Pull Request
- Еженедельно (воскресенье, 00:00 UTC)

### 4. Dependency Review (Обзор зависимостей)
**Файл:** `.github/workflows/dependency-review.yml`

**Что делает:**
- Проверяет новые зависимости в Pull Request
- Блокирует небезопасные зависимости

**Когда запускается:**
- При создании Pull Request

### 5. Auto Fix (Автоисправление)
**Файл:** `.github/workflows/auto-fix.yml`

**Что делает:**
- Автоматически исправляет проблемы с линтером
- Коммитит исправления в PR

**Когда запускается:**
- При создании Pull Request
- Вручную через GitHub Actions UI

### 6. Stale Issues/PRs (Старые задачи)
**Файл:** `.github/workflows/stale.yml`

**Что делает:**
- Помечает неактивные issues как "stale" через 60 дней
- Закрывает неактивные issues через 7 дней после пометки
- То же самое для Pull Requests

**Когда запускается:**
- Ежедневно в полночь (UTC)

## 🔧 Настройка GitHub Actions

### 1. Добавьте Secrets в GitHub

Зайдите в `Settings → Secrets and variables → Actions` и добавьте:

**Обязательные:**
```
DATABASE_URL
NEXTAUTH_SECRET
NEXTAUTH_URL
MEILISEARCH_HOST
MEILISEARCH_MASTER_KEY
```

**Опциональные (для деплоя на Vercel):**
```
VERCEL_TOKEN
VERCEL_ORG_ID
VERCEL_PROJECT_ID
```

### 2. Включите Dependabot

Dependabot автоматически включится при наличии файла `.github/dependabot.yml`.

Он будет:
- Автоматически проверять обновления зависимостей еженедельно
- Создавать Pull Request с обновлениями
- Группировать обновления по типу (production/development)

### 3. Настройте Branch Protection

Рекомендуется включить защиту веток для `main`:

1. Перейдите в `Settings → Branches`
2. Добавьте правило для `main`:
   - ✅ Require a pull request before merging
   - ✅ Require status checks to pass before merging
   - ✅ Select: `lint`, `test`, `build`
   - ✅ Require branches to be up to date

## 📝 Шаблоны Issues и Pull Requests

### Bug Report
Используйте шаблон `.github/ISSUE_TEMPLATE/bug_report.md` для сообщений об ошибках.

### Feature Request
Используйте шаблон `.github/ISSUE_TEMPLATE/feature_request.md` для запросов функций.

### Pull Request
При создании Pull Request автоматически будет использован шаблон из `.github/pull_request_template.md`.

## ✅ Чеклист перед первым запуском

- [ ] Создать репозиторий на GitHub
- [ ] Настроить Secrets (см. выше)
- [ ] Запушить код в репозиторий
- [ ] Проверить, что workflows запустились в разделе `Actions`
- [ ] Настроить Branch Protection для `main`
- [ ] Включить Dependabot (автоматически при наличии `dependabot.yml`)

## 🐛 Решение проблем

### Workflow не запускается
- Проверьте синтаксис YAML файлов
- Убедитесь, что файлы находятся в `.github/workflows/`
- Проверьте права доступа к репозиторию

### Тесты падают
- Проверьте логи в разделе `Actions`
- Убедитесь, что все зависимости установлены
- Проверьте, что переменные окружения настроены правильно

### Деплой не работает
- Проверьте, что все необходимые Secrets настроены
- Убедитесь, что Vercel подключен к GitHub репозиторию
- Проверьте логи деплоя в Vercel Dashboard

---

**Подробнее:** [GitHub Actions Documentation](https://docs.github.com/en/actions)


