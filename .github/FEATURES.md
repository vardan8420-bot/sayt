# ✨ GitHub Features и автоматизация

Проект использует различные функции GitHub для улучшения процесса разработки.

## 🤖 GitHub Actions Workflows

### CI/CD Pipeline

#### 1. CI Workflow (`.github/workflows/ci.yml`)
- **Линтинг:** Автоматическая проверка кода с ESLint
- **Типы:** Проверка TypeScript типов
- **Тесты:** Запуск unit и E2E тестов
- **Сборка:** Проверка, что приложение собирается
- **Безопасность:** Проверка уязвимостей (npm audit)

#### 2. Deploy Workflow (`.github/workflows/deploy.yml`)
- **Автоматический деплой** на Vercel при push в `main`
- **Проверка сборки** перед деплоем
- **Переменные окружения** из GitHub Secrets

### Качество кода

#### 3. CodeQL Analysis (`.github/workflows/codeql.yml`)
- **Анализ безопасности** кода
- **Обнаружение уязвимостей** в зависимостях
- **Еженедельный** автоматический анализ

#### 4. Dependency Review (`.github/workflows/dependency-review.yml`)
- **Проверка новых зависимостей** в Pull Request
- **Блокировка** небезопасных пакетов
- **Предупреждения** о потенциальных проблемах

#### 5. Auto Fix (`.github/workflows/auto-fix.yml`)
- **Автоматическое исправление** проблем с линтером
- **Коммит исправлений** прямо в Pull Request
- **Экономия времени** на ручное исправление

### Управление репозиторием

#### 6. Stale Issues/PRs (`.github/workflows/stale.yml`)
- **Помечает неактивные** issues и PR как "stale"
- **Закрывает старые** задачи автоматически
- **Ежедневная проверка** неактивности

---

## 📝 Шаблоны Issues и Pull Requests

### Issue Templates

#### Bug Report (`.github/ISSUE_TEMPLATE/bug_report.md`)
- Стандартизированный формат сообщений об ошибках
- Чеклист для воспроизведения проблемы
- Поля для окружения и логов

#### Feature Request (`.github/ISSUE_TEMPLATE/feature_request.md`)
- Структурированный запрос функций
- Описание проблемы и решения
- Приоритет функции

### Pull Request Template (`.github/pull_request_template.md`)
- Чеклист перед созданием PR
- Описание изменений
- Связанные issues

---

## 🔄 Dependabot

### Автоматические обновления (`.github/dependabot.yml`)
- **Еженедельная проверка** обновлений npm пакетов
- **Группировка** обновлений (production/development)
- **Автоматические Pull Request** с обновлениями
- **Проверка безопасности** новых версий

---

## 🔒 Безопасность

### GitHub Security Features
- **Dependabot Alerts** - уведомления об уязвимостях
- **Code Scanning** - анализ кода на уязвимости
- **Secret Scanning** - поиск закоммиченных секретов
- **Dependency Review** - проверка зависимостей в PR

---

## 📊 Insights и аналитика

### Доступные метрики:
- **Contributors** - список контрибьюторов
- **Traffic** - просмотры и клоны репозитория
- **Commits** - история коммитов
- **Code frequency** - частота изменений
- **Network** - граф веток и коммитов
- **Pulse** - активность за последние 7 дней

---

## 🎯 Best Practices

### 1. Ветвление
- `main` - продакшен версия
- `develop` - разработка
- `feature/*` - новые функции
- `fix/*` - исправления ошибок
- `hotfix/*` - срочные исправления

### 2. Коммиты
Используйте конвенциональные коммиты:
- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - документация
- `style:` - форматирование
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - обновления зависимостей

### 3. Pull Requests
- Всегда создавайте PR для изменений в `main`
- Используйте шаблон PR
- Привязывайте к issues (`Closes #123`)
- Запрашивайте ревью
- Дождитесь прохождения CI

### 4. Issues
- Используйте шаблоны
- Присваивайте labels
- Назначайте assignees
- Устанавливайте milestones

---

## 🚀 Workflow процесса разработки

1. **Создать issue** для новой функции/исправления
2. **Создать ветку** от `main`: `git checkout -b feature/my-feature`
3. **Внести изменения** и закоммитить
4. **Запушить ветку**: `git push origin feature/my-feature`
5. **Создать Pull Request** с описанием
6. **Дождаться CI** проверок
7. **Получить ревью** от коллег
8. **Исправить замечания** (если есть)
9. **Merge PR** после одобрения
10. **Автоматический деплой** на продакшен

---

## 🔧 Настройка и кастомизация

### Настройка workflows:
Все workflows находятся в `.github/workflows/` и могут быть кастомизированы под ваши нужды.

### Добавление новых проверок:
1. Создайте новый workflow файл в `.github/workflows/`
2. Настройте triggers (`on:`)
3. Добавьте steps
4. Push в репозиторий

### Настройка Secrets:
1. Зайдите в `Settings` → `Secrets and variables` → `Actions`
2. Добавьте необходимые Secrets
3. Используйте в workflows: `${{ secrets.SECRET_NAME }}`

---

**Подробнее:** См. [GITHUB-SETUP.md](../GITHUB-SETUP.md) для полной инструкции по настройке.


