# Настройка GitHub аутентификации

## 📋 Шаги для настройки

### 1. Создайте OAuth App на GitHub

1. Перейдите на https://github.com/settings/developers
2. Нажмите "New OAuth App"
3. Заполните форму:
   - **Application name**: Sayt (или ваше название)
   - **Homepage URL**: `http://localhost:3000` (для разработки)
   - **Authorization callback URL**: `http://localhost:3000/api/auth/callback/github`
4. Нажмите "Register application"

### 2. Получите Client ID и Secret

После создания приложения вы увидите:
- **Client ID** - скопируйте его
- **Client Secret** - нажмите "Generate a new client secret" и скопируйте

### 3. Добавьте в .env.local

```env
GITHUB_CLIENT_ID="ваш-client-id"
GITHUB_CLIENT_SECRET="ваш-client-secret"
```

### 4. Для Production

Когда будете деплоить на production:

1. Создайте новый OAuth App для production
2. Обновите URLs:
   - **Homepage URL**: `https://your-domain.com`
   - **Authorization callback URL**: `https://your-domain.com/api/auth/callback/github`
3. Обновите переменные окружения на сервере

## ✅ Проверка

После настройки:

1. Перезапустите сервер: `npm run dev`
2. Откройте `/api/auth/signin`
3. Должна появиться кнопка "Sign in with GitHub"
4. Нажмите и авторизуйтесь через GitHub

## 🔒 Безопасность

- ✅ Client Secret хранится в `.env.local` (не коммитится в git)
- ✅ OAuth токены безопасно обрабатываются NextAuth
- ✅ Сессии хранятся в базе данных через Prisma

## 📝 Примечания

- GitHub OAuth работает только с HTTPS в production
- Для localhost можно использовать HTTP
- После первой авторизации пользователь автоматически создается в БД

