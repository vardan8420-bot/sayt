# 🚀 Настройка GitHub для проекта Sayt

Пошаговая инструкция по настройке GitHub Actions, автоматизации и деплоя.

## 📋 Предварительные требования

1. Аккаунт на GitHub
2. Локальный репозиторий проекта
3. Git установлен локально

---

## 1️⃣ Создание репозитория на GitHub

### Шаг 1: Создайте новый репозиторий

1. Зайдите на [github.com](https://github.com)
2. Нажмите `+` → `New repository`
3. Заполните:
   - **Repository name:** `sayt` (или ваше имя)
   - **Description:** "Modern Marketplace Platform"
   - **Visibility:** Public или Private (на ваше усмотрение)
   - **НЕ** ставьте галочку "Initialize with README" (у нас уже есть файлы)

4. Нажмите `Create repository`

### Шаг 2: Подключите локальный репозиторий

```bash
# Если репозиторий ещё не инициализирован
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Modern Marketplace Platform"

# Добавьте remote (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/sayt.git

# Или через SSH (если настроен)
git remote add origin git@github.com:YOUR_USERNAME/sayt.git

# Запушьте код
git branch -M main
git push -u origin main
```

---

## 2️⃣ Настройка GitHub Secrets

### Шаг 1: Откройте настройки Secrets

1. Зайдите в ваш репозиторий на GitHub
2. Перейдите в `Settings` → `Secrets and variables` → `Actions`
3. Нажмите `New repository secret`

### Шаг 2: Добавьте обязательные Secrets

Добавьте следующие секреты (одно за раз):

#### База данных
```
Name: DATABASE_URL
Value: postgresql://user:password@host:5432/sayt
```

#### NextAuth
```
Name: NEXTAUTH_SECRET
Value: [сгенерируйте: openssl rand -base64 32]
```

```
Name: NEXTAUTH_URL
Value: https://yourdomain.com
```

#### Meilisearch
```
Name: MEILISEARCH_HOST
Value: https://your-meilisearch-instance.com
```

```
Name: MEILISEARCH_MASTER_KEY
Value: your-master-key
```

### Шаг 3: Опциональные Secrets (для расширенного функционала)

#### Stripe (для платежей)
```
Name: STRIPE_SECRET_KEY
Value: sk_live_...
```

```
Name: STRIPE_PUBLISHABLE_KEY
Value: pk_live_...
```

#### Vercel (для автоматического деплоя)
```
Name: VERCEL_TOKEN
Value: [из Vercel Dashboard → Settings → Tokens]
```

```
Name: VERCEL_ORG_ID
Value: [из Vercel Dashboard]
```

```
Name: VERCEL_PROJECT_ID
Value: [из Vercel Dashboard]
```

#### Другие сервисы
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`
- `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET`
- `OPENAI_API_KEY`
- `SENTRY_DSN`
- и т.д.

---

## 3️⃣ Проверка GitHub Actions

### Шаг 1: Проверьте, что workflows созданы

После push кода в репозиторий:

1. Зайдите в раздел `Actions` вашего репозитория
2. Вы должны увидеть workflows в левом меню:
   - CI
   - Deploy
   - CodeQL Analysis
   - и т.д.

### Шаг 2: Запустите workflow вручную

1. Создайте тестовый commit и push:
```bash
git commit --allow-empty -m "test: trigger GitHub Actions"
git push
```

2. Зайдите в `Actions` → выберите workflow `CI`
3. Дождитесь завершения (должны пройти все проверки)

### Шаг 3: Проверьте результаты

- ✅ **Зелёная галочка** = всё прошло успешно
- ❌ **Красный крестик** = есть ошибки (проверьте логи)

---

## 4️⃣ Настройка Branch Protection

Защитите основную ветку от прямых коммитов:

1. Зайдите в `Settings` → `Branches`
2. Нажмите `Add rule`
3. В поле `Branch name pattern` введите: `main`
4. Включите:
   - ✅ `Require a pull request before merging`
   - ✅ `Require status checks to pass before merging`
     - Выберите: `lint`, `test`, `build`
   - ✅ `Require branches to be up to date before merging`
5. Нажмите `Create`

Теперь изменения в `main` можно вносить только через Pull Request.

---

## 5️⃣ Настройка автоматического деплоя на Vercel

### Вариант A: Через Vercel Dashboard (Рекомендуется)

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите `Add New Project`
3. Импортируйте ваш GitHub репозиторий
4. Настройте переменные окружения (см. раздел выше)
5. Нажмите `Deploy`

Vercel автоматически будет деплоить при каждом push в `main`.

### Вариант B: Через GitHub Actions

Если хотите использовать наш workflow `.github/workflows/deploy.yml`:

1. Получите токены из Vercel:
   - Зайдите в Vercel Dashboard → Settings → Tokens
   - Создайте новый токен
   - Скопируйте `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`

2. Добавьте их в GitHub Secrets (см. раздел выше)

3. Workflow автоматически запустится при push в `main`

---

## 6️⃣ Включение Dependabot

Dependabot автоматически включится, так как у нас есть файл `.github/dependabot.yml`.

Он будет:
- ✅ Еженедельно проверять обновления npm пакетов
- ✅ Создавать Pull Request с обновлениями
- ✅ Группировать обновления (production/development)

Чтобы проверить:
1. Зайдите в `Settings` → `Code security and analysis`
2. Убедитесь, что `Dependabot alerts` и `Dependabot security updates` включены

---

## 7️⃣ Использование шаблонов Issues и PR

### Создание Issue с шаблоном:

1. Зайдите в раздел `Issues`
2. Нажмите `New issue`
3. Выберите шаблон:
   - 🐛 **Bug report** - для сообщений об ошибках
   - ✨ **Feature request** - для запросов функций
4. Заполните форму и создайте issue

### Создание Pull Request:

1. Создайте новую ветку:
```bash
git checkout -b feature/my-feature
```

2. Внесите изменения и закоммитьте:
```bash
git add .
git commit -m "feat: add new feature"
git push origin feature/my-feature
```

3. На GitHub создайте Pull Request
4. Шаблон автоматически заполнится в описании PR

---

## ✅ Чеклист настройки

- [ ] Репозиторий создан на GitHub
- [ ] Код запушен в репозиторий
- [ ] Все Secrets добавлены в GitHub Settings
- [ ] GitHub Actions workflows запускаются успешно
- [ ] Branch Protection настроена для `main`
- [ ] Dependabot включен
- [ ] Деплой на Vercel настроен (опционально)
- [ ] Тестовый issue и PR созданы

---

## 🔍 Проверка работоспособности

### Проверка CI:

```bash
# Создайте тестовый commit
git commit --allow-empty -m "test: verify CI workflow"
git push
```

Проверьте в `Actions` → `CI` - должен пройти успешно.

### Проверка деплоя:

```bash
# Создайте новый feature branch
git checkout -b test/deploy
git commit --allow-empty -m "test: verify deployment"
git push origin test/deploy

# Создайте PR в main
# После merge PR, деплой должен запуститься автоматически
```

---

## 🐛 Решение проблем

### Workflow не запускается
- ✅ Проверьте, что файлы находятся в `.github/workflows/`
- ✅ Проверьте синтаксис YAML (можно проверить на [yamllint.com](https://yamllint.com))
- ✅ Убедитесь, что путь к файлам правильный

### Тесты падают
- ✅ Проверьте логи в разделе `Actions`
- ✅ Запустите тесты локально: `npm run lint && npm run test:unit`
- ✅ Убедитесь, что все зависимости обновлены: `npm ci`

### Secrets не работают
- ✅ Убедитесь, что Secrets добавлены в `Settings → Secrets and variables → Actions`
- ✅ Проверьте правильность названий (чувствительны к регистру)
- ✅ Перезапустите workflow после добавления Secrets

### Деплой не работает
- ✅ Проверьте, что все Secrets для деплоя настроены
- ✅ Проверьте логи деплоя в Vercel Dashboard
- ✅ Убедитесь, что Vercel подключен к GitHub репозиторию

---

## 📚 Полезные ссылки

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Vercel Documentation](https://vercel.com/docs)
- [Dependabot Documentation](https://docs.github.com/en/code-security/dependabot)

---

**Готово! 🎉 Теперь у вас настроена полная автоматизация в GitHub!**


