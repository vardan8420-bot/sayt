# 🚀 Финальная инструкция для деплоя

## ✅ Все исправления готовы!

- ✅ Проект успешно собирается (`npm run build`)
- ✅ Исправлены пути импорта CSS модулей
- ✅ Исправлен `vercel.json` (удалены некорректные rewrites)
- ✅ Все файлы закоммичены и готовы к push

---

## 📋 Пошаговая инструкция (5 минут):

### Шаг 1: Откройте Vercel

Откройте в браузере: **https://vercel.com/new**

### Шаг 2: Авторизуйтесь через GitHub

1. Нажмите **"Continue with GitHub"**
2. Войдите в GitHub (если еще не авторизованы)
3. Разрешите доступ Vercel к вашим репозиториям

### Шаг 3: Создайте репозиторий на GitHub (если еще не создан)

1. Откройте: **https://github.com/new**
2. **Repository name:** `sayt`
3. Выберите **Private** ✓
4. **НЕ** ставьте галочку "Add a README file"
5. Нажмите **"Create repository"**

### Шаг 4: Отправьте код в GitHub

**Вариант A: Используйте автоматический скрипт**
```powershell
.\push-to-github.ps1
```
Скрипт запросит Personal Access Token.

**Вариант B: Вручную через токен**
1. Создайте токен: https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Выберите права: **repo** ✓
   - Скопируйте токен
2. Выполните:
```powershell
git remote set-url origin https://YOUR_TOKEN@github.com/vardan84/sayt.git
git push -u origin main
```
(Замените `YOUR_TOKEN` на ваш токен)

### Шаг 5: Импортируйте репозиторий в Vercel

1. Вернитесь на https://vercel.com/new
2. В разделе **"Import Git Repository"** выберите **`vardan84/sayt`**
3. Или введите URL: `https://github.com/vardan84/sayt.git`
4. Нажмите **"Import"**

### Шаг 6: Настройте проект на Vercel

Vercel автоматически определит:
- ✅ Framework: Next.js
- ✅ Build Command: `npm run build`
- ✅ Install Command: `npm install`

**Просто нажмите:** **"Deploy"** (или "Continue" → "Deploy")

### Шаг 7: Дождитесь деплоя (2-3 минуты)

Vercel автоматически:
- Установит зависимости
- Соберет проект
- Задеплоит на CDN

### Шаг 8: Настройте переменные окружения (после первого деплоя)

1. Откройте проект на Vercel
2. **Settings** → **Environment Variables**
3. Добавьте необходимые переменные (см. ниже)

---

## 🌐 После деплоя:

Ваш сайт будет доступен по адресу:
- `https://sayt-*.vercel.app` (автоматически)
- Или ваш кастомный домен (если настроите)

---

## 🔧 Переменные окружения (опционально):

После первого деплоя добавьте в Vercel:

**Минимальные для работы:**
```
DATABASE_URL=postgresql://user:password@host:5432/sayt
NEXTAUTH_SECRET=сгенерируйте_ключ
NEXTAUTH_URL=https://ваш-проект.vercel.app
NODE_ENV=production
```

**Для полного функционала:**
```
MEILISEARCH_HOST=https://your-meilisearch.com
MEILISEARCH_MASTER_KEY=your_key
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_live_...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
GITHUB_CLIENT_ID=...
GITHUB_CLIENT_SECRET=...
OPENAI_API_KEY=...
```

После добавления переменных:
- Перейдите в **Deployments**
- Нажмите **⋯** на последнем деплое
- Выберите **"Redeploy"**

---

## ✅ Проверка работы:

После деплоя проверьте:
- ✅ Главная страница загружается
- ✅ API маршруты работают (`/api/products`)
- ✅ Страницы авторизации открываются (`/auth/signin`)

---

## 🎉 Готово!

**Ваш сайт теперь онлайн! 🚀**

Если возникнут проблемы:
- Проверьте логи в Vercel: **Deployments** → выберите деплой → **Logs**
- Убедитесь, что все переменные окружения настроены
- Проверьте, что репозиторий на GitHub существует и доступен

